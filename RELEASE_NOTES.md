# v2.0.1 测试版

- 文件篮新增完整多选操作：支持空白区域拖框、`⌘A` 全选、`⌘`/`Shift` 点选，以及把全部蓝色选中项目一次拖到 Finder、微信或其它 App；复制和删除键也会作用于全部选中项目。
- 修复文件篮方向键导航：网格视图可用左右键移动一项、上下键移动一行，列表视图四个方向键均可切换相邻项目。
- 文件项目右键菜单新增隔空投送、邮件和信息，支持一次分享当前多选集合。
- 文件篮默认改为三列缩略图网格，并提供网格/列表切换；图片、视频与文档使用 Quick Look 生成内容预览，失败时回到系统文件图标。
- 文件篮界面参考 macOS 原生文件管理层级重新整理：标题区直接显示项目数量和总体积，全选时不再让每一项同时展开操作按钮，并强化系统强调色选中反馈。
- 修复测试版展示字符串被误当成语义版本，导致“无法识别当前 App 版本”且不能在线更新的问题。
- 设置页新增“接收内测版本”：正式版可直接加入或退出内测；内测通道会同时检查 App、Premiere 插件和 DaVinci 脚本测试版。
- 内测发布改用 `v<版本>-beta.<构建号>` 与 GitHub prerelease，同一版本内可按构建号持续升级；正式版仍只接收稳定 Release。
- 文件篮现支持普通文件、文件夹与 macOS 包；这些项目均可暂存、跨启动恢复、拖出、复制、预览和在 Finder 中定位。
- 篮内包含文件夹或包时，发送按钮会明确切换为“压缩发送”，输入 ZIP 名称后再发送；仅含普通文件时仍保持立即发送。
- DaVinci Resolve 21 及更高版本已知限制：使用脚本自动发送前必须关闭「偏好设置 → 后台任务 → 启用后台渲染」；开启后 `BgRenderManager` 回调可能缺少 Deliver `JobId`、状态和错误上下文，导致自动发送无法可靠工作。该问题已向 Blackmagic Design 提交（工单 `DEV-11860`），等待官方修复。
- 文件篮中的普通文件支持直接拖出到 Finder、微信及其他接受文件 URL 的 App，拖出后仍保留篮内引用。
- 选中文件后可按 `⌘C` 复制真实文件；文件行、右键菜单及文件篮「…」菜单同步增加复制入口。
- 保留「复制全部文件路径」，并新增「复制全部文件」，明确区分文件剪贴板与文本路径。
- 修复拖出文件时误移动整个文件篮窗口；窗口仅能从顶部标题栏区域拖动。

# v2.0.0 测试版

- “暂存架”升级并更名为“文件篮”，支持同时创建多个独立悬浮窗口。
- 每个文件篮独立保存文件、位置、折叠和置顶状态；主面板可快速新建、显示全部、关闭全部或删除指定篮。
- 文件篮管理菜单新增“删除全部文件篮”，统一确认后批量移除文件引用，不会删除磁盘原文件。
- 统一优化文件篮出现、收起、折叠、提示、选中和按钮反馈：窗口保留低回弹 spring，高频操作改用短促无回弹过渡。
- 关闭空文件篮会自动删除；窗口内新增清空与删除入口，删除非空篮前会确认且不会删除磁盘原文件。
- 文件篮「…」菜单新增自定义名称后压缩 ZIP 发送、复制全部路径和在 Finder 中显示全部；ZIP 发送不改变篮内文件。
- 摇晃新建文件篮会从指针旁以低回弹弹簧平滑放大；松手时空篮自动收起，并遵循系统“减少动态效果”设置。
- `⌥⌘S` 显示或隐藏最近使用的文件篮；拖着文件左右摇晃时，会在鼠标附近新建文件篮。
- 菜单栏拖入仍然立即发送；批量发送只处理当前文件篮，首版仍仅支持普通文件。
- 网站 `/dl/dmg`、`/dl/zip` 下载链路修复，避免点下载却回到首页；并补充静态重定向兜底。
- 网站导航增加飞书「更新与反馈」入口，便于查看版本日志与讨论。
- DaVinci Resolve 后渲染双入口：`WeClawSend_Lua`（推荐，curl，无需 Python）与 `WeClawSend_Python`（可选）。
- DaVinci 后台渲染兼容：无 Deliver `JobId` 的内部后台任务回调会被忽略，不再误报失败；真实 Deliver 任务仍按 `JobId` 自动发送，并补充 Resolve API 句柄获取路径。
- 移除设置页「Python 为 DaVinci 运行依赖」的检测与安装引导；默认不再把 Python 当作必装项。
- 安装默认 dual，自动清理旧 `自动发送ClawBot_*` 与过渡脚本名；支持用户/系统 Deliver 双目录；CLI 支持 `both|lua|python|auto`。
- MP4/M4V 显示名由 App「发送时 .mp4 显示为 .m4v」处理，脚本不再分模式。
- 设置增加「发送结果通知」开关；多文件发送结果合并为一条系统通知（同标识替换，不逐条刷屏）。

# v1.7.1

- 设置页为 Premiere 插件与 DaVinci 脚本增加「卸载」。
- DaVinci 旁增加 Python 3 检测、「重新检测」与「安装指南」（打开官网 / 复制 Homebrew 安装命令）。
- App 只引导安装 Python，不会替你安装或卸载系统 / Homebrew Python，避免误清环境。
- 完善 README、使用说明与 DaVinci 文档中的完整 Python 安装步骤：检查 `python3 --version`、Homebrew（含首次安装 Homebrew）与官方安装包；推荐 3.11/3.12。
- 使用说明补充自行卸载 Python 的方式：Homebrew `brew uninstall` 与官网安装包清理路径，并明确不要删除 macOS 自带 `/usr/bin/python3`。

# v1.7.0

- DaVinci Resolve 脚本安装后会校验目标文件是否真实存在，避免“显示成功但目录为空”。
- 设置页安装成功后展示用户 Deliver 路径，并支持“显示路径”在 Finder 中打开。
- 使用说明补充 Python 3.6+ 要求、官方/Homebrew 安装方式，以及安装后必须彻底重启 Resolve。
- 明确脚本安装到当前用户目录，并补充下拉只有“无”时的排查步骤。

# v1.6.11

- 微信独立登录改为三个明确步骤：扫码确认、在 ClawBot 发送消息完成会话绑定、连接完成。
- 只有取得可发送文件的微信会话后才显示“已连接”，不再把扫码成功误认为全部完成。
- 发现新版本时会显示更新内容小弹窗；关闭后当前版本不再重复提醒。
- Release 现在强制携带对应版本的中文更新说明。
